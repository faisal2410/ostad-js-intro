 /**
  * Add/Remove elements from the array =======>
  * ====================================
  * 1.use push to add element to an array as the last element array
  * 2.use pop to remove last element
  * 3.use unshift to add element to an array as the first element array
  * 4.use shift to remove first element
  * */

var numbers = [78, 45, 98, 45];
// numbers.push(80)
// numbers.pop()

// console.log(numbers);

var nums=[25,35]
// nums.unshift(99)
nums.shift();


console.log(nums);