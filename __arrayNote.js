/**
 * 1. Declaration of Array
 * 2.array index, elements, length
 * 3.get element value by index
 * 4.set element value by index
 * 5.find index of an element -indexOf(element)
 * */ 

/**
 * Add/remove last element from an array using push and pop (push to add and pop to remove)
 * Add/remove first element from an array using unshift and shift (unshift to add and shift to remove)
 * 
 * 
 * */ 

